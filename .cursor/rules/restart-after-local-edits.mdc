---
description: After editing relayer, UI, or local scripts, restart dev services (do not only tell the user).
globs: zk-stables-relayer/**/*,zk-stables-ui/**/*,scripts/**/*
alwaysApply: false
---

# Restart local dev services after changes

When you change code under `zk-stables-relayer/`, `zk-stables-ui/`, or `scripts/` for this repo, **run the restart script from the repo root** (if the environment has shell access and Docker/network are available):

```bash
./scripts/restart-local-dev-services.sh
```

This restarts the **relayer** and **Vite** so new TypeScript and UI code load. It does **not** restart Hardhat, Yaci, Midnight, or proof-server; run `./scripts/start-local-stack.sh` or `./scripts/start-yaci-devkit.sh` when those layers need a full bring-up.

Do not end the turn with “you should restart” alone when you can execute the script yourself.

## zk-stables-ui: production build + `vite preview` on port **4173**

When you **modify any file under `zk-stables-ui/`** in this repo, finish the same turn by **rebuilding the UI and restarting preview** (user workflow: validate the built bundle on **http://localhost:4173/**):

```bash
fuser -k 4173/tcp 2>/dev/null || true
sleep 1
cd zk-stables-ui && npm run build
cd zk-stables-ui && npx vite preview --host 0.0.0.0 --port 4173
```

Run the last command **in the background** if your tool supports it (preview stays up). Do not skip the **`npm run build`** step after TS/TSX/CSS changes.
